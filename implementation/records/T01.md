# TASK 实施记录：T01

- 任务ID / 规范版本与hash：T01 / 提示包 `spec_version 1.0.0`；`AGENTS.md` sha256 `fe52caa1dbdd89ae139a2cd11cfdf6f5b6d3dab7eec7bdd04e8d2aa2eea0cf5a`；`specs/03` 副本见 `implementation/specification/specs/03-build-and-dependencies.md`
- 实际生产目录 / branch / HEAD / dirty snapshot：`D:\axiom-colab` / `v1-rebuild-prep` / HEAD 见本轮提交（本文件不自引用）/ 开工前工作树干净（porcelain 0 行）
- 模型或客户端 / 日期 / Windows与工具链：DSH coding agent（`deepseek-v4.1-flash-expires-on-0910`，host session `session-0b7bd31d-b86b-4874-9548-5f419ba03a2d`）/ 2026-09-09 / Windows 11 build 26200.9278 x64；VS Build Tools 2026 `18.9.12120.119`；MSVC `14.51.36231`（cl `19.51.36256.0`，link `14.51.36256.0`）；Windows SDK `10.0.26100.0`；CMake `4.3.1-msvc1`；Ninja `1.13.2`；Qt `6.11.2 msvc2022_64`
- 前置门禁和授权依据：T00 已完成（G00 的构建/测试部分为 `not_run`，见 T00 记录）。用户 2026-09-09 批准：工具链版本切换（C4-a）、Qt 安装（C4-b）、开源许可方向（C4-c → AGPL-3.0-or-later）、保留 `D:\axiom-colab`（C1）、依赖预取于 `D:\axiom-colab\deps\sources`。原文见 `implementation/records/APPROVALS-2026-09-09.md`。

## 文件状态

| 文件ID | 路径 | 原hash | 新hash | 状态 | 对应需求和证据 |
| --- | --- | --- | --- | --- | --- |
| F007 | `CMakeLists.txt` | `fb874553…` 为 `.gitignore`；本文件原为 legacy `CMakeLists.txt`（`general_ts_cpp_adapter`） | 见 git 提交 | written + built | 显式 targets/源码清单/依赖方向/分阶段开启；V1/V2 |
| F008 | `CMakePresets.json` | 不存在 | 见 git 提交 | written + built | win-debug/win-release 三组；V1–V5 |
| F009 | `cmake/Dependencies.cmake` | 不存在 | 见 git 提交 | written + tested | 离线位置与 hash/版本门禁；V4/V7 |
| F010 | `cmake/Warnings.cmake` | 不存在 | 见 git 提交 | written + tested | `/W4 /permissive- /utf-8 /EHsc /fp:precise`、`/MDd`/`/MD`；V2 |
| F011 | `cmake/Packaging.cmake` | 不存在 | 见 git 提交 | written（install 规则未被 T01 调用执行） | 带许可证的 install 规则；T18 才补产物清单 |
| F012 | `deps/lock.json` | 不存在 | 见 git 提交 | written + tested | 真实字节 hash/来源/许可；V4 |
| F013 | `implementation/toolchain.json` | 不存在 | 见 git 提交 | written | 真实 cl/SDK/cmake/ninja/Qt 路径与 fingerprint；V6 |
| F014 | `docs/BUILD.md` | 不存在 | 见 git 提交 | written；F5 项 awaiting_user | VS 打开/F5 与命令行同源构建 |
| F015 | `docs/LICENSE-DECISION.md` | 不存在 | 见 git 提交 | written | AGPL-3.0-or-later 与证据；根 `LICENSE` 未创建（非 T01 文件） |
| F016 | `cmake/BootstrapDependencies.cmake` | 不存在 | 见 git 提交 | written + tested | 只从锁定版本预取并核对字节；V3 |
| — | `tools/bootstrap-deps.ps1` | 不存在 | 见 git 提交 | written + tested | F016 的实现体（脚本，非 CMake 命令字符串） |
| — | `tests/support/axh1.{hpp,cpp}`、`tests/contract/*.cpp` | 不存在 | 见 git 提交 | written + tested | T01 构建门禁测试；V5 |

状态口径：`written` 文件已写；`built` 关联 target 编译成功；`tested` 独立实际检查通过。

## 实际验证

### V1 configure（win-debug）

```text
cd /d D:\axiom-colab
cmake --preset win-debug
exit=0
-- The CXX compiler identification is MSVC 19.51.36256.0
-- deps/lock.json loaded from D:/axiom-colab/cmake/../deps
-- axiom: phase=m0 audio=OFF
-- axiom: registered targets: axiom_test_support;axiom_build_contract_tests
-- axiom: pending targets (not implemented yet): axiom_domain (phase m1); … ;physical_string_tools (phase m4)
-- Configuring done (0.1s) / Generating done (0.1s)
```

### V2 build（win-debug）

```text
cmake --build --preset win-debug
exit=0
[107/115] Building C object CMakeFiles\axiom_sqlite3.dir\deps\sources\sqlite\sqlite3.c.obj
[115/115] Linking CXX executable axiom_build_contract_tests.exe
```

编译命令行证据（第一方 target）：
`/std:c++20 -MDd /W4 /permissive- /utf-8 /EHsc /fp:precise /Zc:__cplusplus /Zc:preprocessor …`
→ 与 `specs/03` 的 `/MDd`（Debug）、`/W4 /permissive- /utf-8`、`/fp:precise` 一致。

### V3 依赖预取与字节核对（bootstrap）

```text
GET https://github.com/nlohmann/json/releases/download/v3.12.0/json.tar.xz
  sha256=42f6e95cad6ec532fd372391373363b62a14af6d771056dbfc86160e6dfff7aa bytes=114576
GET https://github.com/catchorg/Catch2/archive/refs/tags/v3.8.1.tar.gz
  sha256=18b3f70ac80fccc340d8c6ff0f339b2ae64944782f8d2fca2bd705cf47cadb79 bytes=1197623
GET https://www.sqlite.org/2026/sqlite-amalgamation-3530400.zip
  sha256=1e71ddf93849c6a6ecf58b827c0692073d2dd7ee40196158068f7b29f422e87d bytes=2946650
```

官方摘要交叉核对（`contracts/dependencies-baseline.json`）：

```text
sqlite3.c sha3-256 = 67f423e9ebbbdc473cbc4772c872ee6b89f31fde4ed0279a5c25d5f65c043a16
expected           = 67f423e9ebbbdc473cbc4772c872ee6b89f31fde4ed0279a5c25d5f65c043a16   MATCH
sqlite3.c sha256   = b1dd5d74ec7f29055a6684fa06fb3c2f6821c87dd38f9a458dfd2e8a1db28189
```

注：baseline 字段名是 `official_sqlite3_c_sha3_256`，因此核对必须用 SHA3-256；本机 .NET Framework 无 SHA3 实现，改用 Python 3.14 `hashlib.sha3_256` 计算。

`deps/lock.json` 记录的树摘要（去除 marker 后按相对路径排序的 `path\tsha256\tsize` 文本再 SHA-256）：

```text
nlohmann_json  tree_sha256=8f5e9a0414dd3060cabfb0d81664f170c2329360e1ba5bbbe5660b64bd8e002e files=55
catch2         tree_sha256=c1f95462a36846d4a87c95c85d0d1779b16e88bbc291b6f361488657f628b574 files=559
sqlite         tree_sha256=d7c6b8935ca493fff703ac98986932ff1b54028f6e300e3a2e5d299532fdbf0a files=5
```

### V4 离线门禁

`cmake/Dependencies.cmake` 的 `axiom_require_dependency()` 检查 `.axiom-lock-verified` 标记与 `deps/lock.json` 的 sha256 一致；不一致或缺失即 `FATAL_ERROR`。`deps/sources/*/.axiom-lock-verified` 已写入对应归档摘要。configure 期间无任何网络访问。

### V5 测试门禁（ctest，debug 与 release）

```text
ctest --preset win-debug
1/1 Test #1: axiom_build_contract .............   Passed    3.01 sec
100% tests passed, 0 tests failed out of 1        exit=0
Label Time Summary: build = … domain = …

ctest --preset win-release
1/1 Test #1: axiom_build_contract .............   Passed    0.01 sec
100% tests passed, 0 tests failed out of 1        exit=0
```

`win-release` 以 `AXIOM_WARNINGS_AS_ERRORS=ON` 编译第一方目标（`/WX`），无警告。

测试内容（`tests/contract/`）：
1. 工具链为 MSVC、64 位、`_MSC_VER`/`_MSC_FULL_VER` 存在；
2. `__cplusplus >= 202002L`、concepts、三路比较可用；
3. 无 `_M_FP_FAST`（`/fp:precise`）、`-0.0` 归一化；
4. 源码按 UTF-8 编译（`"\u957F"` 字节为 E9 95 BF）；
5. 锁定依赖版本：nlohmann/json 3.12.0、SQLite 3.53.4；
6. 12 条 AXH1 golden 向量（`contracts/hash-golden.json`）逐条比对编码字节与摘要，含负零、UTF-8、对象键序；
7. SHA-256 空串与 `"abc"` 标准向量；
8. SQLite 编译定义 `SQLITE_OMIT_LOAD_EXTENSION`。

**过程中被测试真实纠正的两个认知（不得当作已实现）：**
- nlohmann/json **默认不拒绝重复 key**（保留最后一个）。`specs/04` 要求拒绝重复 key，因此这是 C01 校验器（T03）的责任，不是解析器特性；测试已改为只验证键序与解析错误。
- Catch2 3.8.1 声明但**未定义** `StringMaker<std::byte>::convert`，因此断言中不得出现 `std::byte` 容器；相关断言改为比较十六进制字符串。

### V6 工具链 fingerprint

```text
canonical JSON (556 bytes) = {compiler{id,cl_version,link_version,cl_exe},sdk,cmake{version,sha256},ninja{version,sha256},qt{version,kit,build_compiler_msvc,linkage}}
sha256 = 5c57fb574985d6e575037d4146390b958e92460577cd173a1e411d65a487e211
```

### V7 Qt 6.11.2 强制检查（specs/03 M0）

```text
set CMAKE_PREFIX_PATH=C:/Qt/6.11.2/msvc2022_64
cmake --preset win-release          # AXIOM_REQUIRE_QT=ON
-- axiom: Qt 6.11.2 verified from C:/Qt/6.11.2/msvc2022_64/lib/cmake/Qt6
-- Configuring done / Generating done        exit=0
```

Qt 组件仅为 Core/Network/Gui/Widgets；`Qt6::Sql`/`Qt6::Qml` 若被引入即 `FATAL_ERROR`（`specs/03` 禁止）。

## 剩余事项与交接

### not_run / not_implemented

- 12 个业务组件、`axiom-worker`、`axiom-mcp`、`axiom-review`、`axiom-diagnostics`、DSP/音频目标：全部 **not_implemented**，configure 时列为 pending（源码未交付），不是被跳过的成功。
- `cmake --install`（F011 的 install 规则）未执行：dist 产物属 T18。
- JUCE 8.0.14 未预取（T15 需要），因此 `deps/lock.json` 尚无 JUCE 条目；F012 会在 T15 前补齐。

### VS IDE 运行证据（用户实际执行，2026-09-09）

用户报告（原文）：

```text
Randomness seeded to: 4148975135
===============================================================================
All tests passed (50 assertions in 10 test cases)

for axiom_build_contract_tests
```

独立复跑（本模型执行，非引用用户输出）：

```text
build\win-debug\axiom_build_contract_tests.exe
  -> Randomness seeded to: 453237351
  -> All tests passed (50 assertions in 10 test cases)
  -> exit=0

ctest --preset win-debug
  -> 1/1 Test #1: axiom_build_contract ... Passed 0.02 sec
  -> 100% tests passed, 0 tests failed out of 1   exit=0
```

关联事实（消除“跑的是别的二进制”的可能）：

| 项 | 值 |
| --- | --- |
| 二进制 | `D:\axiom-colab\build\win-debug\axiom_build_contract_tests.exe`，4,905,984 字节，sha256 `ac4af3247ea271b7390cafe3ddb0d90c96fefc6f1a21b149c43d65bb6c08d519` |
| 构建配置 | `CMAKE_BUILD_TYPE=Debug`，生成器 Ninja |
| 编译器 | `E:/Microsoft Visual Studio/VC/Tools/MSVC/14.44.35207/bin/HostX64/x64/cl.exe`（冻结的 14.44） |
| 断言/用例数 | 50 assertions / 10 test cases（与用户报告一致） |

**证据等级**：`tested`（可复现的真实执行）。

用户补充确认（2026-09-09）：**通过 VS Insiders 2026 的 F5 运行 `win-debug` 配置，未命中断点。**

| 子项 | 状态 | 证据 |
| --- | --- | --- |
| VS Insiders 2026 打开 `D:\axiom-colab`（CMake 文件夹模式） | **passed** | 用户报告：F5 启动 `win-debug` 配置并完成运行 |
| F5 启动目标可执行文件并跑完 | **passed** | 50 assertions / 10 test cases 全通过（用户输出 + 本模型独立复跑） |
| 断点命中与变量观察（`A03`/`A04` 要求） | **awaiting_user** | 用户明确报告 **no breakpoint hit**：无断点、无变量观察记录 |

因此：VS 的**构建与启动**路径已由真实 IDE 运行证明；**调试（断点）**路径仍未证明。
不得把「F5 跑通测试」当作 `A03`/`A04` 通过——那两项要求的是实际断点命中与变量观察。

建议的最小补证（供用户执行，任选其一）：

1. 在 `tests/contract/hash_golden_test.cpp` 的 `REQUIRE(to_hex(prefixed) == expected_hex);` 行设断点，
   用 F5 运行 `axiom_build_contract_tests`，命中断点后记录 `name`、`expected_hex` 与 `to_hex(prefixed)` 的值；
2. 或在 `tests/support/axh1.cpp` 的 `sha256_hex()` 入口设断点，记录 `bytes.size()`。

记录内容需包含：IDE 版本（18.10.12120.281）、断点文件与行号、命中时的变量值、截图或日志引用。

### 已完成（用户后续指令补齐，非 T01 原始文件清单）

| 项 | 证据 |
| --- | --- |
| 根 `LICENSE`（AGPL-3.0 全文，661 行，sha256 `8486a10c4393cee1c25392769ddd3b2d6c242d6ec7928e1414efff7dfb2f07ef`） | 提交 `2860646` |
| `deps/THIRD-PARTY-NOTICES.md`（nlohmann_json MIT / Catch2 BSL-1.0 / SQLite public domain / Qt LGPLv3 / JUCE 待核实） | 提交 `2860646` |
| `main` fast-forward 到 V1 树（新增 75、删除 0） | 提交 `d3848c1`、`c758263`、`1be7b05`；见 `migration-plan.md` §7 |

仍待 T18：第三方通知与 Qt 许可文本随包安装的端到端验证（`cmake/Packaging.cmake` 已支持）。

### blocked / 未关闭冲突

| ID | 事项 | 状态 |
| --- | --- | --- |
| C2 | 本地规则要求读 `D:\Dev\tools\*` 参考工程，本机 `D:\Dev` 不存在 | 仍未关闭 |
| C3 | 本地规则要求每次提交更新 `for-agent/HANDOFF.md`，与包规则 16 冲突 | 仍未关闭，本轮按包规则 16 |
| JUCE 条款 | JUCE 8.0.14 的 `LICENSE.md` 未取得（DNS 阻止 juce.com / raw.githubusercontent.com） | 待 T15 从锁定字节核对 |
| ABI 记录 | Qt 由 MSVC 19.44 构建、本机编译器 19.51 | 已记录于 `toolchain.json`，须由 T02/T13 真实链接+运行证明 |

### DCR

- 本轮**没有**提交 DCR，也没有 `approved` 状态记录。工具链版本变更的正式 DCR 落盘路径尚未定义（`FILE-MANIFEST.csv` 无 DCR 条目），需用户裁定后补记。

### 下一文件 / 下一任务

- T01 文件清单已全部交付；G01 的 configure/build/test 为 `passed`。
- 下一任务：**T02**（阶段 M0，前置 T01）——先验证 Windows 隔离与音频入口。按 `tasks/INDEX.md`，T02 沙箱失败会阻塞所有候选执行路径。
- 尚需读取：`specs/05-authority-and-isolation.md`（已读）、`tasks/T02.md`、`contracts/command-catalog.json` 相关段。

### 本轮没有改变的规范

- 未修改 `implementation/specification/**` 任何字节；未修改验收 oracle、容差或测试用例以适配实现；无禁用失败用例。
- `specs/03` 冻结文本未改（工具链版本变更记录在 `APPROVALS-2026-09-09.md` 与 `toolchain.json`）。
