# T04 实现对象库和SQLite权威存储

你是此任务的实现者。本次只实施 T04；阶段 M1；前置任务 T03。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/06-storage-and-recovery.md`
- `specs/05-authority-and-isolation.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

建两类DB的schema1/RAII、路径守卫、对象staging与revision。用真实SQLite与临时NTFS目录测试，先将持久化原语完成。

不使用QtSql/ORM/第二状态文件；不能先记succeeded再落字节。跨工作区访问必须拒绝。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F039 | `libs/store/sqlite.hpp` | SQLite handle/statement/transaction RAII与schema版本检查；仅公开/私有类型声明，禁止实现跨层业务 |
| F040 | `libs/store/sqlite.cpp` | SQLite handle/statement/transaction RAII与schema版本检查；按同名头文件实现，不增加公共入口 |
| F041 | `libs/store/object_store.hpp` | staging、内容寻址、chunk读取与配额；仅公开/私有类型声明，禁止实现跨层业务 |
| F042 | `libs/store/object_store.cpp` | staging、内容寻址、chunk读取与配额；按同名头文件实现，不增加公共入口 |
| F043 | `libs/store/workspace_repository.hpp` | 对象revision/working/artifact/audit typed查询；仅公开/私有类型声明，禁止实现跨层业务 |
| F044 | `libs/store/workspace_repository.cpp` | 对象revision/working/artifact/audit typed查询；按同名头文件实现，不增加公共入口 |
| F045 | `libs/store/path_guard.hpp` | Windows最终handle路径、重解析点与大小写冲突检查；仅公开/私有类型声明，禁止实现跨层业务 |
| F046 | `libs/store/path_guard.cpp` | Windows最终handle路径、重解析点与大小写冲突检查；按同名头文件实现，不增加公共入口 |
| F047 | `libs/store/migrations/workspace_v1.sql` | workspace schema1规范建表 |
| F048 | `libs/store/migrations/library_v1.sql` | 独立library schema1规范建表 |
| F049 | `tests/store_tests.cpp` | revision冲突、原子可见性、非法路径、配额、schema拒绝 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T04.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L store；CAS冲突、容量满、路径逃逸、损坏、未知schema、文件与DB提交失败注入。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
