# T10 实现候选验证与独立证据

你是此任务的实现者。本次只实施 T10；阶段 M2；前置任务 T07, T09。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/08-tools-and-validation.md`
- `specs/06-storage-and-recovery.md`
- `specs/05-authority-and-isolation.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现candidate/build recipe、三类suite、可信oracle与证据分流。用户设置私有challenge，通过只给单次输入的worker通信检验。

不信任作者测试自己说PASS；不泄漏私有oracle；缺suite/隔离失败/退出异常须incomplete或failed。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F101 | `libs/validation/candidate.hpp` | immutable候选和精确build recipe绑定；仅公开/私有类型声明，禁止实现跨层业务 |
| F102 | `libs/validation/candidate.cpp` | immutable候选和精确build recipe绑定；按同名头文件实现，不增加公共入口 |
| F103 | `libs/validation/coordinator.hpp` | 受信runner观察、suite执行、二进制hash结论；仅公开/私有类型声明，禁止实现跨层业务 |
| F104 | `libs/validation/coordinator.cpp` | 受信runner观察、suite执行、二进制hash结论；按同名头文件实现，不增加公共入口 |
| F105 | `libs/validation/oracle.hpp` | typed_exact/numeric_tolerance/artifact_properties参考检查；仅公开/私有类型声明，禁止实现跨层业务 |
| F106 | `libs/validation/oracle.cpp` | typed_exact/numeric_tolerance/artifact_properties参考检查；按同名头文件实现，不增加公共入口 |
| F107 | `libs/validation/evidence.hpp` | 公开/私有证据隔离、持久化及完整性；仅公开/私有类型声明，禁止实现跨层业务 |
| F108 | `libs/validation/evidence.cpp` | 公开/私有证据隔离、持久化及完整性；按同名头文件实现，不增加公共入口 |
| F109 | `tests/validation_tests.cpp` | 伪PASS/失败退出/缺suite/旧二进制/私密泄露均拒绝 |
| F110 | `tests/fixtures/dishonest_worker.cpp` | 仅测试协议谎称通过/返回坏内容，受控运行 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `libs/host/host_service.cpp`：仅装配本任务服务并实现catalog中已有对应命令，不改角色/批准规则。

此外只能更新implementation/records/T10.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L validation；dishonest worker、旧hash、oracle保密、超时、required suite缺失均有失败用例。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
