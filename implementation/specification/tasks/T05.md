# T05 实现Host身份和本地通信

你是此任务的实现者。本次只实施 T05；阶段 M1；前置任务 T04。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/05-authority-and-isolation.md`
- `specs/10-mcp-and-ipc.md`
- `specs/06-storage-and-recovery.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现framing/HostClient/session/hostservice/jobqueue；未实现业务命令返回unsupported。review capability用继承私有pipe，MCP固定model角色；后续服务通过host composition root装配。

不得接受caller自报actor/admin、落盘secret或让UI直接写DB。不能用空成功响应伪装未来命令。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F050 | `libs/ipc/framing.hpp` | QLocalSocket长度帧/有界解析/事件序列；仅公开/私有类型声明，禁止实现跨层业务 |
| F051 | `libs/ipc/framing.cpp` | QLocalSocket长度帧/有界解析/事件序列；按同名头文件实现，不增加公共入口 |
| F052 | `libs/ipc/host_client.hpp` | UI/MCP异步传输客户端，无业务授权判断；仅公开/私有类型声明，禁止实现跨层业务 |
| F053 | `libs/ipc/host_client.cpp` | UI/MCP异步传输客户端，无业务授权判断；按同名头文件实现，不增加公共入口 |
| F054 | `libs/host/session.hpp` | host授予model/reviewer身份与workspace能力；仅公开/私有类型声明，禁止实现跨层业务 |
| F055 | `libs/host/session.cpp` | host授予model/reviewer身份与workspace能力；按同名头文件实现，不增加公共入口 |
| F056 | `libs/host/host_service.hpp` | typed命令分发、service composition root、权威串行通道；仅公开/私有类型声明，禁止实现跨层业务 |
| F057 | `libs/host/host_service.cpp` | typed命令分发、service composition root、权威串行通道；按同名头文件实现，不增加公共入口 |
| F058 | `libs/host/job_queue.hpp` | 每workspace并发1、幂等提交、cancel/poll与真实状态；仅公开/私有类型声明，禁止实现跨层业务 |
| F059 | `libs/host/job_queue.cpp` | 每workspace并发1、幂等提交、cancel/poll与真实状态；按同名头文件实现，不增加公共入口 |
| F060 | `apps/host/main.cpp` | 单host实例、启动review的私有能力管道、参数与退出 |
| F061 | `tests/authority_tests.cpp` | actor伪造/跨workspace/重放/错误批准主体拒绝 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T05.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L authority；非法role/重放/跨workspace/过大frame/连接中断/相同key异内容测试。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
