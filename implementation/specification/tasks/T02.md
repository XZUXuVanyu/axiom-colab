# T02 先验证Windows隔离与音频入口

你是此任务的实现者。本次只实施 T02；阶段 M0；前置任务 T01。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/05-authority-and-isolation.md`
- `specs/03-build-and-dependencies.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

先实现Windows sandbox原语与受控probe。真实启动隔离CMake编译小程序并验证网络/文件/进程树/超时。建立JUCE静音壳验证可产生Standalone/VST3；不写物理算法。

AppContainer/CMake失败是实际架构阻碍。不得用普通Process替代；记录错误码、ACL、最小复现并提DCR。音频空壳明确不是V1完成。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F017 | `libs/runner/windows_sandbox.hpp` | AppContainer token/ACL/security attributes与Job启动原语；仅公开/私有类型声明，禁止实现跨层业务 |
| F018 | `libs/runner/windows_sandbox.cpp` | AppContainer token/ACL/security attributes与Job启动原语；按同名头文件实现，不增加公共入口 |
| F019 | `tests/fixtures/sandbox_probe.cpp` | 受控probe尝试文件/网络/子进程/资源行为，绝无真实破坏 |
| F020 | `tests/platform_probe.cpp` | 真实受限CMake编译调用与取消，oracle在父进程 |
| F021 | `examples/audio-smoke/CMakeLists.txt` | JUCE空壳M0构建入口，不包含正式DSP |
| F022 | `examples/audio-smoke/PluginProcessor.cpp` | 临时静音processor检查VST3/Standalone入口，明确非最终产品 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T02.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest标签platform_probe；实际受限编译和安全探针必须通过才解锁候选生产路径。音频界面人工启动可awaiting_user。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
