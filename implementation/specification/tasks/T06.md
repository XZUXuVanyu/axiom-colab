# T06 实现Tool SDK和两个基础工具

你是此任务的实现者。本次只实施 T06；阶段 M1；前置任务 T03。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/08-tools-and-validation.md`
- `specs/09-library-and-workflow.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现ITool、registry、DI、worker协议与Scale/Offset。公开tool类与普通内部服务区分。工厂注册数据驱动，不把名字判断写进MCP。

不新增任意动态DLL loader、跨调用singleton或隐式依赖扫描。基础工具仅系统probe。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F062 | `libs/sdk/tool.hpp` | ITool/CallContext/输入输出和SDK版本；仅公开/私有类型声明，禁止实现跨层业务 |
| F063 | `libs/sdk/tool.cpp` | ITool/CallContext/输入输出和SDK版本；按同名头文件实现，不增加公共入口 |
| F064 | `libs/sdk/registry.hpp` | 显式factory/服务依赖拓扑/unique_ptr装配；仅公开/私有类型声明，禁止实现跨层业务 |
| F065 | `libs/sdk/registry.cpp` | 显式factory/服务依赖拓扑/unique_ptr装配；按同名头文件实现，不增加公共入口 |
| F066 | `libs/sdk/worker_protocol.hpp` | 单次worker请求、staging访问、结构化结果；仅公开/私有类型声明，禁止实现跨层业务 |
| F067 | `libs/sdk/worker_protocol.cpp` | 单次worker请求、staging访问、结构化结果；按同名头文件实现，不增加公共入口 |
| F068 | `apps/worker/main.cpp` | SDK demo worker入口，启动后一次请求退出 |
| F069 | `examples/probe-tools/probe_tools.hpp` | ScaleTool与OffsetTool两个公开类，仅系统样例；仅公开/私有类型声明，禁止实现跨层业务 |
| F070 | `examples/probe-tools/probe_tools.cpp` | ScaleTool与OffsetTool两个公开类，仅系统样例；按同名头文件实现，不增加公共入口 |
| F071 | `examples/probe-tools/tool-descriptors.json` | 两probe的精确类型/权限/界限 |
| F072 | `tests/sdk_tests.cpp` | 缺依赖/循环/非法输入/独立worker无共享状态 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T06.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L sdk；缺依赖、依赖环、输出schema、退出/协议、两worker状态独立。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
