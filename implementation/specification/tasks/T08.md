# T08 实现标准MCP与插件目录

你是此任务的实现者。本次只实施 T08；阶段 M2；前置任务 T05, T06, T07。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/10-mcp-and-ipc.md`
- `specs/05-authority-and-isolation.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

完成固定协议与catalog投影，声明真正实现的能力。生成安装模板和Codex manifest/skill说明，stdout无污染。动态tool绑定变化发list_changed。

不自创JSONRPC子协议、不开HTTP、不声明未实现能力、不写全局客户端配置。单客户端实测不可替其他模型认证。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F080 | `libs/mcp/json_rpc.hpp` | stdio换行JSONRPC、ID无损、固定错误码和取消；仅公开/私有类型声明，禁止实现跨层业务 |
| F081 | `libs/mcp/json_rpc.cpp` | stdio换行JSONRPC、ID无损、固定错误码和取消；按同名头文件实现，不增加公共入口 |
| F082 | `libs/mcp/gateway.hpp` | initialize/tools/list/tools/call、动态绑定与schema投影；仅公开/私有类型声明，禁止实现跨层业务 |
| F083 | `libs/mcp/gateway.cpp` | initialize/tools/list/tools/call、动态绑定与schema投影；按同名头文件实现，不增加公共入口 |
| F084 | `apps/mcp/main.cpp` | 仅stdio MCP到host的model连接，workspace启动参数 |
| F085 | `tests/mcp_tests.cpp` | 真实子进程协议一致性、分页、非法JSON和越权方法 |
| F086 | `plugins/axiom-colab/.codex-plugin/plugin.json` | 固定元数据/skills/MCP companion引用 |
| F087 | `plugins/axiom-colab/.mcp.json.in` | 安装时生成真实路径和workspace，不含能力secret |
| F088 | `plugins/axiom-colab/skills/axiom-workflow/SKILL.md` | 工具调用流程与用户批准边界，外部客户端无需本包历史 |
| F089 | `docs/MCP-CLIENT.md` | 真实客户端安装与配置检查，不冒称模型均兼容 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `libs/host/host_service.cpp`：仅装配本任务服务并实现catalog中已有对应命令，不改角色/批准规则。

此外只能更新implementation/records/T08.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L mcp；真实子进程initialize/list/call/pagination/cancel/错误，人工客户端连接另记。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
