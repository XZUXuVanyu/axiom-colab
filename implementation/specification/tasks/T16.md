# T16 开发离线音频工具与实验组合

你是此任务的实现者。本次只实施 T16；阶段 M4；前置任务 T15。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/13-analysis-tools.md`
- `specs/09-library-and-workflow.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现四公开Tool、artifact编码、独立分析测试并通过Axiom发布。搭正式分叉汇合图，封装一层组合到库，W2复用另组参数。

不把peak_frequency标成理论f1，不把包络拟合强称gamma，不用限幅数据验证DSP。不能第二份物理算法。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F157 | `examples/physical-string/tools/string_render_tool.hpp` | 共享DSP原速度序列+试听WAV+metadata；仅公开/私有类型声明，禁止实现跨层业务 |
| F158 | `examples/physical-string/tools/string_render_tool.cpp` | 共享DSP原速度序列+试听WAV+metadata；按同名头文件实现，不增加公共入口 |
| F159 | `examples/physical-string/tools/spectrum_tool.hpp` | 有界Hann FFT单边幅度/peak_frequency；仅公开/私有类型声明，禁止实现跨层业务 |
| F160 | `examples/physical-string/tools/spectrum_tool.cpp` | 有界Hann FFT单边幅度/peak_frequency；按同名头文件实现，不增加公共入口 |
| F161 | `examples/physical-string/tools/decay_tool.hpp` | 窗口RMS对数OLS、not_estimable/growing；仅公开/私有类型声明，禁止实现跨层业务 |
| F162 | `examples/physical-string/tools/decay_tool.cpp` | 窗口RMS对数OLS、not_estimable/growing；按同名头文件实现，不增加公共入口 |
| F163 | `examples/physical-string/tools/report_tool.hpp` | 同源输入核对与JSON/Markdown证据报告；仅公开/私有类型声明，禁止实现跨层业务 |
| F164 | `examples/physical-string/tools/report_tool.cpp` | 同源输入核对与JSON/Markdown证据报告；按同名头文件实现，不增加公共入口 |
| F165 | `examples/physical-string/tools/audio_artifacts.hpp` | 固定little-endian f64原数据/float32 WAV编码与元数据；仅公开/私有类型声明，禁止实现跨层业务 |
| F166 | `examples/physical-string/tools/audio_artifacts.cpp` | 固定little-endian f64原数据/float32 WAV编码与元数据；按同名头文件实现，不增加公共入口 |
| F167 | `examples/physical-string/tools/worker_main.cpp` | 注册四公开工具与service、使用同一SDK worker协议 |
| F168 | `examples/physical-string/tools/descriptors.json` | 四tool精确端口、schema、参数、权限、模型假设 |
| F169 | `examples/physical-string/workflows/string_experiment.json` | 真实工具版本在发布后解析冻结的组合模板 |
| F170 | `examples/physical-string/tests/analysis_tests.cpp` | FFT独立DFT/包络参考/原数据与报告来源检查 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `examples/physical-string/CMakeLists.txt`：仅追加四工具/worker/analysis测试，复用已登记DSP target。

此外只能更新implementation/records/T16.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L analysis、P10及真实workflow；raw/preview/metadata/report来源hash一致。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
