# T17 实现Standalone和通用VST3

你是此任务的实现者。本次只实施 T17；阶段 M4；前置任务 T15, T16。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/12-physical-audio.md`
- `specs/11-review-ui.md`
- `specs/15-delivery.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

JUCE外壳链接库中精确DSP。实现APVTS、GUI、MIDI、sample offsets、5ms gain/Stop、状态恢复、独立Standalone/VST3。实际生成包后分别测宿主。

回调不分配/锁/落盘/MCP；绝不复制DSP。不能只交WAV/空壳或改成Max。缺DAW标awaiting_user。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F171 | `examples/physical-string/plugin/PluginProcessor.hpp` | JUCE bus/MIDI offsets/纯DSP回调/prepare与release；仅公开/私有类型声明，禁止实现跨层业务 |
| F172 | `examples/physical-string/plugin/PluginProcessor.cpp` | JUCE bus/MIDI offsets/纯DSP回调/prepare与release；按同名头文件实现，不增加公共入口 |
| F173 | `examples/physical-string/plugin/PluginEditor.hpp` | 物理参数GUI/Pluck Stop/有效T/波形计数；仅公开/私有类型声明，禁止实现跨层业务 |
| F174 | `examples/physical-string/plugin/PluginEditor.cpp` | 物理参数GUI/Pluck Stop/有效T/波形计数；按同名头文件实现，不增加公共入口 |
| F175 | `examples/physical-string/plugin/ParameterState.hpp` | 固定APVTS IDs/状态schema/有界参数和命令通道；仅公开/私有类型声明，禁止实现跨层业务 |
| F176 | `examples/physical-string/plugin/ParameterState.cpp` | 固定APVTS IDs/状态schema/有界参数和命令通道；按同名头文件实现，不增加公共入口 |
| F178 | `examples/physical-string/tests/audio_adapter_tests.cpp` | sample offset/float映射/状态恢复/回调无分配测试 |
| F179 | `examples/physical-string/docs/DAW-CHECKLIST.md` | Live/FL真实版本与人工操作证据入口 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `CMakePresets.json`：将最终音频target开启，保留Debug/Release和固定ABI。
- `examples/physical-string/CMakeLists.txt`：仅追加JUCE Standalone/VST3与adapter测试，复用已登记DSP/tools targets。

此外只能更新implementation/records/T17.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L audio；P06–P09人工清单含10min耗时、underrun、Live/FL版本和host退出独立播放。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
