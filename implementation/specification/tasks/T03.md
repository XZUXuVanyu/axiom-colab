# T03 实现领域数据与固定契约

你是此任务的实现者。本次只实施 T03；阶段 M1；前置任务 T01。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/04-contracts-and-science.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

按contracts拆分强类型ID、record、error、schema、science、hash、state与ports。契约先于下游实现。对支持子集严格拒绝未知关键字，编码为AXH1。

不新增泛型单位代数/反射/通用数学引擎。不可用Qt JSON绕过整数精度，不把非法枚举转默认值。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F023 | `libs/domain/ids.hpp` | 强类型ID/revision/hash与无损解析；仅公开/私有类型声明，禁止实现跨层业务 |
| F024 | `libs/domain/ids.cpp` | 强类型ID/revision/hash与无损解析；按同名头文件实现，不增加公共入口 |
| F025 | `libs/domain/error.hpp` | 错误枚举与Result类型；仅公开/私有类型声明，禁止实现跨层业务 |
| F026 | `libs/domain/error.cpp` | 错误枚举与Result类型；按同名头文件实现，不增加公共入口 |
| F027 | `libs/domain/records.hpp` | data-model记录和序列化，闭合字段校验；仅公开/私有类型声明，禁止实现跨层业务 |
| F028 | `libs/domain/records.cpp` | data-model记录和序列化，闭合字段校验；按同名头文件实现，不增加公共入口 |
| F029 | `libs/domain/schema.hpp` | 固定JSON Schema子集与typed value校验；仅公开/私有类型声明，禁止实现跨层业务 |
| F030 | `libs/domain/schema.cpp` | 固定JSON Schema子集与typed value校验；按同名头文件实现，不增加公共入口 |
| F031 | `libs/domain/science.hpp` | V1单位/量种/策略/未知不确定度元数据；仅公开/私有类型声明，禁止实现跨层业务 |
| F032 | `libs/domain/science.cpp` | V1单位/量种/策略/未知不确定度元数据；按同名头文件实现，不增加公共入口 |
| F033 | `libs/domain/hash.hpp` | AXH1规范编码与BCrypt SHA-256；仅公开/私有类型声明，禁止实现跨层业务 |
| F034 | `libs/domain/hash.cpp` | AXH1规范编码与BCrypt SHA-256；按同名头文件实现，不增加公共入口 |
| F035 | `libs/domain/state.hpp` | 合法状态转移与结果判定；仅公开/私有类型声明，禁止实现跨层业务 |
| F036 | `libs/domain/state.cpp` | 合法状态转移与结果判定；按同名头文件实现，不增加公共入口 |
| F037 | `libs/domain/ports.hpp` | IToolInvoker/IProcessRunner/IWorkspaceRepository隔离端口定义 |
| F038 | `tests/domain_tests.cpp` | 独立AXH1 golden、无损整数、单位/状态非法路径 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T03.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L domain；独立golden覆盖key顺序、负零、UTF8、整数边界、单位和状态拒绝。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
