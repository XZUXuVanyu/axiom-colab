# T15 通过Axiom流程开发物理DSP

你是此任务的实现者。本次只实施 T15；阶段 M4；前置任务 T10, T11, T12, T13。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/12-physical-audio.md`
- `specs/04-contracts-and-science.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

先在已运行Axiom建立实际产品目标和批准设计，用户/agent通过普通项目生成参数及ModalString源码，捕获候选与独立数值验证，再发布精确DSP源/静态库包。

不把先写好的最终DSP补登记成虚构开发流程；不用Euler或随机正弦代替模型；域/tolerance固定。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F151 | `examples/physical-string/dsp/string_parameters.hpp` | 纯物理参数域校验/派生频率/有效模态数；仅公开/私有类型声明，禁止实现跨层业务 |
| F152 | `examples/physical-string/dsp/string_parameters.cpp` | 纯物理参数域校验/派生频率/有效模态数；按同名头文件实现，不增加公共入口 |
| F153 | `examples/physical-string/dsp/modal_string.hpp` | 固定数组解析模态推进、能量/速度输出；仅公开/私有类型声明，禁止实现跨层业务 |
| F154 | `examples/physical-string/dsp/modal_string.cpp` | 固定数组解析模态推进、能量/速度输出；按同名头文件实现，不增加公共入口 |
| F155 | `examples/physical-string/tests/dsp_tests.cpp` | 独立闭式oracle/能量/零激励/投影/块一致性 |
| F156 | `examples/physical-string/docs/MODEL.md` | 方程推导、域、近似、单位、失败与验证证据 |
| F177 | `examples/physical-string/CMakeLists.txt` | 先登记DSP同源target，T16/T17追加tools/Standalone/VST3 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T15.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L dsp及应用内真实candidate suite；P01–P06保存原始double数据和独立oracle对照。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
