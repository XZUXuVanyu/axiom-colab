# T09 实现双向源码协作

你是此任务的实现者。本次只实施 T09；阶段 M2；前置任务 T04, T05, T07。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/07-source-collaboration.md`
- `specs/10-mcp-and-ipc.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现snapshot/context/worktree提案/marker限制/冲突/diff/applyjournal/VS打开。先用小普通CMake工具项目演示A/B两种模式。

不读取未保存IDE缓冲，不自动三方合并覆盖用户。worktree只在批准根下，不暴露私有tests。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F090 | `libs/project/snapshot.hpp` | 已保存字节捕获/设计依赖绑定；仅公开/私有类型声明，禁止实现跨层业务 |
| F091 | `libs/project/snapshot.cpp` | 已保存字节捕获/设计依赖绑定；按同名头文件实现，不增加公共入口 |
| F092 | `libs/project/context.hpp` | 脱敏有界context导出和分块读取；仅公开/私有类型声明，禁止实现跨层业务 |
| F093 | `libs/project/context.cpp` | 脱敏有界context导出和分块读取；按同名头文件实现，不增加公共入口 |
| F094 | `libs/project/revision.hpp` | worktree/白名单/marker/diff/apply journal及冲突；仅公开/私有类型声明，禁止实现跨层业务 |
| F095 | `libs/project/revision.cpp` | worktree/白名单/marker/diff/apply journal及冲突；按同名头文件实现，不增加公共入口 |
| F096 | `libs/project/vs_launcher.hpp` | 固定本机VS入口和项目路径打开，不读IDE缓冲；仅公开/私有类型声明，禁止实现跨层业务 |
| F097 | `libs/project/vs_launcher.cpp` | 固定本机VS入口和项目路径打开，不读IDE缓冲；按同名头文件实现，不增加公共入口 |
| F098 | `tests/project_tests.cpp` | 模式A/B、文件级冲突与多文件恢复不覆盖新改动 |
| F099 | `templates/tool-project/CMakeLists.txt.in` | 普通用户项目依赖exact SDK的CMake模板 |
| F100 | `templates/tool-project/tool.hpp.in` | 公开Tool壳的契约声明，不填领域实现 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `libs/host/host_service.cpp`：仅装配本任务服务并实现catalog中已有对应命令，不改角色/批准规则。

此外只能更新implementation/records/T09.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L project；用户中途改单文件阻止整个apply，多文件中断恢复；VS真实断点待人工证据。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
