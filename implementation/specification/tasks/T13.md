# T13 实现Qt审核界面和节点画布

你是此任务的实现者。本次只实施 T13；阶段 M3；前置任务 T08, T09, T11, T12。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/11-review-ui.md`
- `specs/09-library-and-workflow.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

逐文件构建四入口和GraphicsView画布，用同一host数据模型驱动。用户可审核提案、发布、绑定、封存、备份操作。参数与run版本清楚可见。

不得直接访问DB/runner或做第二份状态机；不可用静态假数据页面当完成。长动作异步。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F127 | `apps/review/main_window.hpp` | 四区域导航与连接/活动任务状态；仅公开/私有类型声明，禁止实现跨层业务 |
| F128 | `apps/review/main_window.cpp` | 四区域导航与连接/活动任务状态；按同名头文件实现，不增加公共入口 |
| F129 | `apps/review/workspace_panel.hpp` | 目标计划/对象/封存和context操作；仅公开/私有类型声明，禁止实现跨层业务 |
| F130 | `apps/review/workspace_panel.cpp` | 目标计划/对象/封存和context操作；按同名头文件实现，不增加公共入口 |
| F131 | `apps/review/library_panel.hpp` | 精确包、绑定、撤销与依赖详情；仅公开/私有类型声明，禁止实现跨层业务 |
| F132 | `apps/review/library_panel.cpp` | 精确包、绑定、撤销与依赖详情；按同名头文件实现，不增加公共入口 |
| F133 | `apps/review/project_panel.hpp` | 文件差异、区域限制、冲突与VS入口；仅公开/私有类型声明，禁止实现跨层业务 |
| F134 | `apps/review/project_panel.cpp` | 文件差异、区域限制、冲突与VS入口；按同名头文件实现，不增加公共入口 |
| F135 | `apps/review/review_panel.hpp` | 用户精确批准/拒绝、私有suite设置、证据详情；仅公开/私有类型声明，禁止实现跨层业务 |
| F136 | `apps/review/review_panel.cpp` | 用户精确批准/拒绝、私有suite设置、证据详情；按同名头文件实现，不增加公共入口 |
| F137 | `apps/review/graph_canvas.hpp` | Qt Graphics View节点边布局与交互；仅公开/私有类型声明，禁止实现跨层业务 |
| F138 | `apps/review/graph_canvas.cpp` | Qt Graphics View节点边布局与交互；按同名头文件实现，不增加公共入口 |
| F139 | `apps/review/parameter_panel.hpp` | 单位范围/参数来源/dirty状态和结果版本；仅公开/私有类型声明，禁止实现跨层业务 |
| F140 | `apps/review/parameter_panel.cpp` | 单位范围/参数来源/dirty状态和结果版本；按同名头文件实现，不增加公共入口 |
| F141 | `apps/review/job_panel.hpp` | 真实异步进度、cancel、受限公开日志；仅公开/私有类型声明，禁止实现跨层业务 |
| F142 | `apps/review/job_panel.cpp` | 真实异步进度、cancel、受限公开日志；按同名头文件实现，不增加公共入口 |
| F143 | `apps/review/main.cpp` | 独立启动转host激活/私有通道接入/Qt app生命周期 |
| F144 | `tests/ui_model_tests.cpp` | 无Widgets业务旁路、参数数据模型与命令授权检查 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。

此外只能更新implementation/records/T13.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L ui_model；实际1280x720/DPI/中文路径/运行中交互检查，人工项保留真实状态。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
