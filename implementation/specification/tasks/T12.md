# T12 实现目标计划和节点执行

你是此任务的实现者。本次只实施 T12；阶段 M3；前置任务 T07, T11。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/09-library-and-workflow.md`
- `specs/04-contracts-and-science.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

实现goal/plan、图校验、freeze、Kahn确定顺序、串行执行、一层组合。通过IToolInvoker接host已解析worker，先probe图再正式audio图。

无环/自动预览/缓存/并行/递归组合；运行中编辑只改草稿。不能以表格替代最终节点UI。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F118 | `libs/workflow/goal_plan.hpp` | 目标/计划审批与预算绑定；仅公开/私有类型声明，禁止实现跨层业务 |
| F119 | `libs/workflow/goal_plan.cpp` | 目标/计划审批与预算绑定；按同名头文件实现，不增加公共入口 |
| F120 | `libs/workflow/graph.hpp` | 节点/端口/边/精确类型校验与语义hash；仅公开/私有类型声明，禁止实现跨层业务 |
| F121 | `libs/workflow/graph.cpp` | 节点/端口/边/精确类型校验与语义hash；按同名头文件实现，不增加公共入口 |
| F122 | `libs/workflow/scheduler.hpp` | 确定拓扑串行、冻结run、独立分支与取消；仅公开/私有类型声明，禁止实现跨层业务 |
| F123 | `libs/workflow/scheduler.cpp` | 确定拓扑串行、冻结run、独立分支与取消；按同名头文件实现，不增加公共入口 |
| F124 | `libs/workflow/composite.hpp` | 一层组合映射、展开trace、精确依赖与权限并集；仅公开/私有类型声明，禁止实现跨层业务 |
| F125 | `libs/workflow/composite.cpp` | 一层组合映射、展开trace、精确依赖与权限并集；按同名头文件实现，不增加公共入口 |
| F126 | `tests/workflow_tests.cpp` | 分叉汇合/环/单位错/运行中草稿/组合深度/新workspace复用 |

## 允许修改的已有共享文件

- `CMakeLists.txt`：只登记本任务已实现源/target/tests和批准的依赖边，不改变全局编译安全选项。
- `libs/host/host_service.cpp`：仅装配本任务服务并实现catalog中已有对应命令，不改角色/批准规则。

此外只能更新implementation/records/T12.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

ctest -L workflow；fanout/join/独立分支失败/下游skipped/取消/类型错/移动节点hash不变/组合新W复用。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
