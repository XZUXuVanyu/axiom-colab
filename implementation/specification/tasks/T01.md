# T01 冻结依赖和普通CMake工程

你是此任务的实现者。本次只实施 T01；阶段 M0；前置任务 T00。

## 开始前

读取AGENTS.md、specs/01-scope.md、specs/02-architecture.md、specs/04-contracts-and-science.md、contracts/public-api.hpp.txt、contracts/data-model.json、contracts/RECORD-INVARIANTS.md、contracts/artifact-types.json、contracts/hash-golden.json、contracts/command-catalog.json、当前implementation/progress.json和前置记录。相关规范如下：

- `specs/03-build-and-dependencies.md`
- `specs/15-delivery.md`

核对用户文件当前hash与任务基线。前置自动门禁未通过时先诊断，不跳过。不要重新设计接口，不新增外部依赖。

## 本任务要实现的行为

逐文件建立构建系统和依赖锁。解析指定版本真实字节/hash，记录MSVC/SDK/Ninja。创建阶段开关，最终目标依赖不变；无网络正式configure。

依赖许可/版本不符不得自动换库；缺软件先输出准确组件，完成其余配置，不购买或改全局环境。

## 逐文件清单

每个文件先列出将定义的已有契约符号及依赖，再实现；检查表只允许文件内私有细节自由设计。新public字段/方法需DCR。文件ID顺序用于本任务导航，不要求尚未定义的跨文件声明能单独链接。

| 文件ID | 精确路径 | 唯一职责 |
| --- | --- | --- |
| F007 | `CMakeLists.txt` | 显式targets、源码清单、依赖方向与最小分阶段开启 |
| F008 | `CMakePresets.json` | win-debug/win-release configure build test三组 |
| F009 | `cmake/Dependencies.cmake` | 精确离线依赖位置与hash/版本门禁 |
| F010 | `cmake/Warnings.cmake` | MSVC/CRT/precise/UTF-8一致选项 |
| F011 | `cmake/Packaging.cmake` | 带许可证的install规则，T18仅补最终产物清单 |
| F012 | `deps/lock.json` | 真实完整依赖hash、许可、来源与构建选项 |
| F013 | `implementation/toolchain.json` | 真实cl/SDK/cmake/ninja/Qt路径与fingerprint |
| F014 | `docs/BUILD.md` | 普通VS打开/F5与命令行同源构建 |
| F015 | `docs/LICENSE-DECISION.md` | 实际许可证据与所有者待决定事项，不代签 |
| F016 | `cmake/BootstrapDependencies.cmake` | 只从锁定官方版本预取并核对字节；不执行模型命令字符串 |

## 允许修改的已有共享文件

无。

此外只能更新implementation/records/T01.md、implementation/progress.json及该任务证据目录。不得更改冻结规范/验收oracle/其他任务业务文件；发现必须修改时先提案并说明依赖。manifest之外的生产源文件禁止创建。

## 检查和证据

检查显式target/source与无TS服务；configure成功或记录具体缺环境。依赖lock禁止无来源SHA。

使用cmake --preset win-debug、cmake --build --preset win-debug和ctest --preset win-debug --output-on-failure，按本任务标签运行必要用例；没有该平台/软件不得伪造命令输出。任务内部单文件完成记录written，关联target编译成功记built，独立实际检查通过记tested；人工项另列awaiting_user。最终汇总门禁见acceptance/MATRIX.csv。

## 交接输出

使用templates/TASK-REPORT.md格式，列每文件状态和确切SHA、实际命令/退出码、证据位置、未通过原因、所需用户动作及下一文件。没有实现的功能明确not_implemented；不要以TODO/恒true/mock替代验收。通过本任务可继续下一个已满足前置的任务，默认需在下一回合开始。
