1. accept. and avoding use _val_name or val_name_. use g_ prefix for global vals, k_ for keywords, m_ for members.
2. always run interacive hub, but do not send prompt yourself
3. make it a general dir name rather than absolute path
